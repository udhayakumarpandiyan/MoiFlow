# Admin portal — production build config (Vite loads this for `vite build`).
# This is a public, non-secret value: the API origin the browser calls.
# Never put secrets in VITE_* vars — they are inlined into the client bundle.
VITE_API_URL=https://api.moiflow.in
